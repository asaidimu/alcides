name: typed
description: Merge type definitions to definitely typed.

inputs:
    gh_token:
        description: 'Authentication for gh'
        required: true
    git_token:
        description: 'Authentication for git'
        required: true

runs:
    using: docker
    image: Dockerfile
