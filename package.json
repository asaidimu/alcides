{
    "private": "true",
    "repository": "git@github.com:asaidimu/alcides.git",
    "author": "saidimu",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "ci": "yarn install --frozen-lockfile",
        "debug": "node --inspect-brk ./dist/index.js",
        "clean": "rm -rf dist",
        "prebuild": "yarn clean",
        "build": "yarn tsc",
        "test": "yarn --silent --offline node ./dist/index.js",
        "build:watch": "yarn tsc --watch",
        "build:docs": "yarn jsdoc -c jsdoc.json",
        "build:docs:watch": "nodemon --exec 'yarn build:docs' --watch src -q",
        "pretest:ci": "yarn build",
        "test:ci": "yarn test",
        "test:push": "yarn build:dist && yarn test --no-watch --parallel && cd ./dist && npm publish --dry-run $PWD && cd ../ && yarn semantic-release",
        "build:dist": "./bin/dist.sh",
        "build:types": "./bin/types.sh"
    },
    "devDependencies": {
        "@semantic-release/changelog": "^6.0.1",
        "@semantic-release/commit-analyzer": "^9.0.2",
        "@semantic-release/git": "^10.0.1",
        "@semantic-release/release-notes-generator": "^10.0.3",
        "@types/alcides": "^1.4.0",
        "@types/chai": "^4.3.0",
        "@types/node": "^17.0.8",
        "@types/yargs": "^17.0.8",
        "alcides": "^1.3.1",
        "better-docs": "^2.7.1",
        "chai": "^4.3.4",
        "jsdoc": "^3.6.9",
        "nodemon": "^2.0.15",
        "prettier": "^2.5.1",
        "semantic-release": "^18.0.1",
        "typescript": "^4.6.0-dev.20220116"
    },
    "release": {
        "branches": [
            "main"
        ],
        "plugins": [
            "@semantic-release/commit-analyzer",
            "@semantic-release/release-notes-generator",
            [
                "@semantic-release/changelog",
                {
                    "changelogFile": "CHANGELOG.md"
                }
            ],
            [
                "@semantic-release/npm",
                {
                    "pkgRoot": "dist"
                }
            ],
            [
                "@semantic-release/git",
                {
                    "assets": [
                        "CHANGELOG.md",
                        "dist.package.json"
                    ],
                    "message": "chore(release): Release v${nextRelease.version}\n\n${nextRelease.notes}"
                }
            ]
        ]
    },
    "dependencies": {
        "chalk": "^5.0.0",
        "fast-glob": "^3.2.11",
        "ora": "^6.0.1",
        "source-map": "^0.7.3",
        "yargs": "^17.3.1"
    }
}
